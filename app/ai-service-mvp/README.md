# 🤖 AI Service Marketplace MVP

**Полностью автономная AI-платформа для связи мастеров с клиентами**

Система на основе искусственного интеллекта, которая без участия операторов:
- ✅ Принимает регистрацию мастеров
- ✅ Обрабатывает заявки клиентов  
- ✅ Ведёт диалог и консультирует
- ✅ Анализирует фото проблем
- ✅ Автоматически подбирает мастера
- ✅ Обрабатывает оплату
- ✅ Рассчитывает заработок

---

## 🚀 Быстрый старт

### Вариант 1: Локальный запуск (рекомендуется для тестирования)

```bash
# 1. Перейти в папку проекта
cd ai-service-mvp

# 2. Запустить сервер
./run.sh
```

Готово! Сервер запустится на `http://localhost:8000`

### Вариант 2: Docker Compose (для продакшена)

```bash
# Запустить все сервисы (backend, postgres, redis, frontend)
docker-compose up -d

# Остановить
docker-compose down
```

---

## 📱 Доступные страницы

| Страница | URL | Описание |
|----------|-----|----------|
| **API документация** | http://localhost:8000/docs | Swagger интерфейс для тестирования API |
| **Форма для клиентов** | http://localhost:3000/index.html | Заявка на вызов мастера |
| **Терминал мастера** | http://localhost:3000/master/terminal.html?id={master_id} | Мобильный терминал для приёма заказов |

---

## 🧪 Тестирование с демо-данными

```bash
# 1. Запустить сервер (если ещё не запущен)
./run.sh

# 2. В НОВОМ терминале запустить создание демо-данных
cd backend
python3 demo.py
```

Скрипт создаст:
- ✅ 3 тестовых мастера (электрик, сантехник, универсал)
- ✅ Расписание работы для каждого
- ✅ Активированные мобильные терминалы
- ✅ Тестовую заявку от клиента

---

## 🎯 Как работает система

### 1️⃣ Регистрация мастера

Мастер регистрируется через **любой канал**:

```bash
# Пример: регистрация через API
curl -X POST http://localhost:8000/api/v1/masters/register \
  -H "Content-Type: application/json" \
  -d '{
    "full_name": "Иванов Иван",
    "phone": "+79001234567",
    "specializations": ["electrical", "plumbing"],
    "city": "Москва",
    "preferred_channel": "telegram"
  }'
```

**Что происходит:**
1. Система собирает информацию о мастере
2. Мастер указывает расписание работы
3. Система активирует мобильный терминал
4. Мастер получает ссылку на терминал

### 2️⃣ Заявка от клиента

Клиент обращается через **любой канал** (сайт, Telegram, звонок):

**Через веб-форму:**
1. Клиент открывает http://localhost:3000/index.html
2. Заполняет форму: категория, описание, адрес
3. Может приложить фото проблемы
4. Отправляет заявку

**Что делает AI:**
1. ✅ Анализирует описание проблемы
2. ✅ Определяет категорию работ
3. ✅ Смотрит фото (если есть)
4. ✅ Оценивает сложность
5. ✅ Рассчитывает стоимость
6. ✅ Создаёт инструкцию для мастера
7. ✅ **Автоматически находит и назначает мастера**

### 3️⃣ Работа мастера

1. Мастер видит заказ в своём терминале
2. Принимает или отклоняет заказ
3. Обновляет статус: "Выехал → В работе → Завершено"
4. Принимает оплату через терминал (карта/СБП/наличные)
5. Система автоматически рассчитывает заработок

---

## 📊 Архитектура системы

```
ai-service-mvp/
├── backend/              # FastAPI приложение
│   ├── app/
│   │   ├── api/         # REST API endpoints
│   │   │   ├── ai.py           # Обработка клиентов
│   │   │   ├── master.py       # Регистрация мастеров
│   │   │   └── terminal.py     # Терминал мастера
│   │   ├── services/    # Бизнес-логика
│   │   │   ├── ai_orchestrator.py   # Главный координатор
│   │   │   ├── nlp_service.py       # Обработка языка
│   │   │   ├── vision_service.py    # Анализ изображений
│   │   │   ├── knowledge_base.py    # База знаний решений
│   │   │   ├── pricing_engine.py    # Расчёт цен
│   │   │   └── master_matcher.py    # Подбор мастера
│   │   ├── models/      # ORM модели
│   │   └── integrations/# Telegram, платежи
│   ├── main.py          # Точка входа
│   └── demo.py          # Создание тестовых данных
├── frontend/            # Веб-интерфейсы
│   ├── index.html              # Форма клиента
│   └── master/terminal.html    # Терминал мастера
└── database/            # SQL схема
```

---

## 🔧 Основные API endpoints

### Мастера

```bash
# Регистрация
POST /api/v1/masters/register

# Обновить профиль
PATCH /api/v1/masters/{id}/profile

# Установить расписание
PUT /api/v1/masters/{id}/schedule

# Активировать терминал
POST /api/v1/masters/{id}/activate-terminal

# Список доступных мастеров
GET /api/v1/masters/available/by-category/{category}
```

### Клиенты (AI)

```bash
# Обработать сообщение (Telegram/чат)
POST /api/v1/ai/messages

# Веб-форма
POST /api/v1/ai/web-form

# Анализ фото
POST /api/v1/ai/analyze/image
POST /api/v1/ai/analyze/images
```

### Терминал мастера

```bash
# Получить заказы
GET /api/v1/terminal/jobs/{master_id}

# Активный заказ
GET /api/v1/terminal/jobs/{master_id}/active

# Принять заказ
POST /api/v1/terminal/jobs/{master_id}/accept/{job_id}

# Обновить статус
PATCH /api/v1/terminal/jobs/{master_id}/status/{job_id}

# Принять оплату
POST /api/v1/terminal/payment/process

# Заработок
GET /api/v1/terminal/earnings/{master_id}
```

---

## 💰 Финансовая модель

**Пример расчёта для заказа 3000₽:**

| Участник | Сумма | % |
|----------|-------|---|
| Клиент платит | 3000₽ | 100% |
| Платёжный шлюз | -60₽ | -2% |
| **Остаток** | **2940₽** | **98%** |
| Комиссия платформы | -735₽ | -25% от остатка |
| **Мастер получает** | **2205₽** | **73.5%** |

Расчёт происходит **автоматически** при обработке платежа.

---

## 🗂️ База знаний

Система содержит готовые решения для:

**Электрика:**
- Розетка не работает
- Выключатель не включает свет
- Автомат выбивает
- Установка люстры

**Сантехника:**
- Течёт кран
- Засор слива
- Унитаз набирает воду

**Бытовая техника:**
- Стиральная машина не сливает воду

Каждое решение включает:
- ✅ Пошаговую инструкцию
- ✅ Список инструментов
- ✅ Список материалов с ценами
- ✅ Меры безопасности
- ✅ Частые ошибки
- ✅ Советы по устранению

---

## ⚙️ Настройка

Скопируйте `.env.example` в `.env` и настройте:

```bash
cp backend/.env.example backend/.env
```

### Основные переменные:

```bash
# База данных (опционально для MVP)
DATABASE_URL=postgresql+asyncpg://user:pass@localhost:5432/db

# Telegram бот (опционально)
TELEGRAM_BOT_TOKEN=your_bot_token

# Платежи YooKassa (опционально)
YOOKASSA_SHOP_ID=your_shop_id
YOOKASSA_SECRET_KEY=your_secret_key

# Комиссия платформы (25% по умолчанию)
PLATFORM_COMMISSION_RATE=0.25
```

**Для MVP все внешние сервисы опциональны!** Система работает в памяти.

---

## 🧪 Тестирование

### Сценарий 1: Создание мастера

```bash
# Создать мастера
curl -X POST http://localhost:8000/api/v1/masters/register \
  -H "Content-Type: application/json" \
  -d '{
    "full_name": "Тестовый Мастер",
    "phone": "+79001111111",
    "specializations": ["electrical"],
    "city": "Москва"
  }'

# Сохранить master_id из ответа
MASTER_ID="полученный_id"

# Установить расписание
curl -X PUT http://localhost:8000/api/v1/masters/$MASTER_ID/schedule \
  -H "Content-Type: application/json" \
  -d '{
    "schedule": {
      "monday": {"available": true, "start_hour": 8, "end_hour": 20}
    }
  }'

# Активировать терминал
curl -X POST http://localhost:8000/api/v1/masters/$MASTER_ID/activate-terminal \
  -H "Content-Type: application/json" \
  -d '{"terminal_type": "mobile"}'
```

### Сценарий 2: Заявка от клиента

```bash
# Отправить заявку
curl -X POST http://localhost:8000/api/v1/ai/web-form \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Клиент Тестовый",
    "phone": "+79002222222",
    "category": "electrical",
    "problem_description": "Не работает розетка",
    "address": "ул. Тестовая 1"
  }'
```

Заказ **автоматически назначится** мастеру!

### Сценарий 3: Работа с терминалом

```bash
# Получить активный заказ мастера
curl http://localhost:8000/api/v1/terminal/jobs/$MASTER_ID/active

# Принять заказ
curl -X POST http://localhost:8000/api/v1/terminal/jobs/$MASTER_ID/accept/$JOB_ID

# Обновить статус
curl -X PATCH http://localhost:8000/api/v1/terminal/jobs/$MASTER_ID/status/$JOB_ID \
  -H "Content-Type: application/json" \
  -d '{"status": "in_progress"}'

# Принять оплату
curl -X POST http://localhost:8000/api/v1/terminal/payment/process \
  -H "Content-Type: application/json" \
  -d '{
    "job_id": "'"$JOB_ID"'",
    "payment_method": "cash"
  }'
```

---

## 📝 Что реализовано

- ✅ **Регистрация мастеров** через любой канал (API, Telegram, форма)
- ✅ **Управление расписанием** - мастер указывает доступность
- ✅ **Мобильный терминал** - PWA приложение для телефона
- ✅ **Приём заявок** - веб-форма, Telegram бот, API
- ✅ **AI диалог** - NLP обработка, уточняющие вопросы
- ✅ **Анализ фото** - Computer Vision для диагностики
- ✅ **База знаний** - 10+ готовых решений с инструкциями
- ✅ **Автоматический расчёт цен** - учёт сложности, материалов, срочности
- ✅ **Автоподбор мастера** - по специализации, доступности, рейтингу
- ✅ **Файл заказа** - полная инструкция для мастера
- ✅ **Приём оплаты** - карта, СБП, наличные
- ✅ **Расчёт заработка** - автоматическое разделение комиссий
- ✅ **In-memory хранилище** - работает без базы данных

---

## 🚧 Roadmap (что можно добавить)

1. **Реальная интеграция**
   - [ ] Yandex SpeechKit для голосовых звонков
   - [ ] YooKassa для реальных платежей
   - [ ] Telegram бот (костяк готов)
   - [ ] WhatsApp Business API

2. **Аналитика**
   - [ ] Дашборд для мастеров
   - [ ] Статистика по заказам
   - [ ] Рейтинговая система

3. **Умные функции**
   - [ ] ML модель для определения проблем по фото
   - [ ] Предиктивная аналитика загрузки мастеров
   - [ ] Динамическое ценообразование

4. **Мобильные приложения**
   - [ ] Native iOS/Android для мастеров
   - [ ] Push-уведомления

---

## 🤝 Поддержка

При возникновении проблем:

1. Проверьте, что сервер запущен: `http://localhost:8000/health`
2. Посмотрите логи в консоли
3. Проверьте API документацию: `http://localhost:8000/docs`

---

## 📄 Лицензия

Этот MVP создан в образовательных целях.

---

**Готово к запуску! 🚀**

Создано с помощью FastAPI, PostgreSQL, Redis и любовью к автоматизации ❤️
